(function(module) {

  module.controller('HelpController', ['$scope','$location','$anchorScroll',
    function($scope,$location,$anchorScroll) {
      $scope.getStarted = false;
      $scope.browserDiv = true;
      $scope.preparationDiv = true;
      $scope.userAccountDiv = true;
      $scope.cxProjectsDiv = true;
      $scope.projectRolesDiv = true;
      $scope.keyDefinitionsDiv = true;
      $scope.features = false;
      $scope.createCXWorkoutDiv = true;
      $scope.editCXWorkoutDiv = true;
      $scope.viewMap = true;
      $scope.createMapDiv = true;
      $scope.createMissionDiv = true;
      $scope.editMissionDiv = true;
      $scope.createListDiv = true;
      $scope.editListDiv = true;
      $scope.reportParticipationDiv = true;
      $scope.needmoreHelpDiv = true;
      $scope.suggetionsDiv = true;
      $scope.practices = false;
      $scope.bestPracticesSupprotsDiv = true;
      $scope.bestPracticesGuildlineDiv = true;

      $scope.expandOnlySelectedTab = function(selectedDiv, isExpanded) {
    	$scope.browserDiv = true;
        $scope.preparationDiv = true;
        $scope.userAccountDiv = true;
        $scope.cxProjectsDiv = true;
        $scope.projectRolesDiv = true;
        $scope.keyDefinitionsDiv = true;
        $scope.suggetionsDiv = true;

        $scope.createCXWorkoutDiv = true;
        $scope.editCXWorkoutDiv = true;
        $scope.viewMap = true;
        $scope.createMapDiv = true;
        $scope.createMissionDiv = true;
        $scope.editMissionDiv = true;
        $scope.createListDiv = true;
        $scope.editListDiv = true;
        $scope.reportParticipationDiv = true;
        $scope.needmoreHelpDiv = true;

        $scope.bestPracticesSupprotsDiv = true;
        $scope.bestPracticesGuildlineDiv = true;
        
        switch (selectedDiv) {
          case 'browserDiv':
            if (isExpanded) {
              $scope.browserDiv = false;
            } else {
              $scope.browserDiv = true;
            }
            break;
          case 'preparationDiv':
            if (isExpanded) {
              $scope.preparationDiv = false;
            } else {
              $scope.preparationDiv = true;
            }
            break;
          case 'userAccountDiv':
            if (isExpanded) {
              $scope.userAccountDiv = false;
            } else {
              $scope.userAccountDiv = true;
            }
            break;
          case 'cxProjectsDiv':
            if (isExpanded) {
              $scope.cxProjectsDiv = false;
            } else {
              $scope.cxProjectsDiv = true;
            }
            break;
          case 'projectRolesDiv':
            if (isExpanded) {
              $scope.projectRolesDiv = false;
            } else {
              $scope.projectRolesDiv = true;
            }
            break;
          case 'keyDefinitionsDiv':
            if (isExpanded) {
              $scope.keyDefinitionsDiv = false;
            } else {
              $scope.keyDefinitionsDiv = true;
            }
            break;
          case 'createCXWorkoutDiv':
            if (isExpanded) {
              $scope.createCXWorkoutDiv = false;
            } else {
              $scope.createCXWorkoutDiv = true;
            }
            break;
          case 'editCXWorkoutDiv':
            if (isExpanded) {
              $scope.editCXWorkoutDiv = false;
            } else {
              $scope.editCXWorkoutDiv = true;
            }
            break;
          case 'viewMap':
            if (isExpanded) {
              $scope.viewMap = false;
            } else {
              $scope.viewMap = true;
            }
            break;
          case 'createMapDiv':
            if (isExpanded) {
              $scope.createMapDiv = false;
            } else {
              $scope.createMapDiv = true;
            }
            break;
           case 'createMissionDiv':
              if (isExpanded) {
                $scope.createMissionDiv = false;
              } else {
                $scope.createMissionDiv = true;
              }
              break;
           case 'editMissionDiv':
               if (isExpanded) {
                 $scope.editMissionDiv = false;
               } else {
                 $scope.editMissionDiv = true;
               }
               break;
           case 'createListDiv':
               if (isExpanded) {
                 $scope.createListDiv = false;
               } else {
                 $scope.createListDiv = true;
               }
               break;
          case 'editListDiv':
              if (isExpanded) {
                $scope.editListDiv = false;
              } else {
                $scope.editListDiv = true;
              }
              break;
          case 'reportParticipationDiv':
              if (isExpanded) {
                $scope.reportParticipationDiv = false;
              } else {
                $scope.reportParticipationDiv = true;
              }
              break;
          case 'needmoreHelpDiv':
            if (isExpanded) {
              $scope.needmoreHelpDiv = false;
            } else {
              $scope.needmoreHelpDiv = true;
            }
            break;
          case 'suggetionsDiv':
            if (isExpanded) {
              $scope.suggetionsDiv = false;
            } else {
              $scope.suggetionsDiv = true;
            }
            break;
          case 'bestPracticesSupprotsDiv':
            if (isExpanded) {
              $scope.bestPracticesSupprotsDiv = false;
            } else {
              $scope.bestPracticesSupprotsDiv = true;
            }
            break;
          case 'bestPracticesGuildlineDiv':
            if (isExpanded) {
              $scope.bestPracticesGuildlineDiv = false;
            } else {
              $scope.bestPracticesGuildlineDiv = true;
            }
            break;
        }


      };

    }
  ]);

})(angular.module('aet.screens.help'));
